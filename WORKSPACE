workspace(name = "Joda_Time")

load("//third_party:workspace.bzl", "maven_dependencies")

# use bazel-deps to manage transitive maven dependencies
# https://github.com/johnynek/bazel-deps
maven_dependencies()
